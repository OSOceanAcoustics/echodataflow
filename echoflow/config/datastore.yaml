name: Bell_M._Shimada-SH1707-EK60
sonar_model: EK60
raw_regex: (.*)-?D(?P<date>\w{1,8})-T(?P<time>\w{1,6})
args:
  urlpath: s3://ncei-wcsd-archive/data/raw/{{ ship_name }}/{{ survey_name }}/{{ sonar_model }}/*.raw
  parameters:
    ship_name: Bell_M._Shimada
    survey_name: SH1707
    sonar_model: EK60
  storage_options:
    anon: true
  transect:
    file: ./Hake.zip
    # file: ./x0007_fileset.txt
    # default_transect_num: 1
  json_export: true
  raw_json_path: s3://echoflow-workground/combined_files/raw_json/Bell_M._Shimada-SH1707-EK60.json
output:
  urlpath: ./combined_files
  # urlpath: s3://echoflow-workground/combined_files
  overwrite: true
  # storage_options:
  #   block_name: echoflow-aws-credentials
  #   type: AWS
